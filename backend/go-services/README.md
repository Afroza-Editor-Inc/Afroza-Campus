# Go Services — Afroza Campus

Rôle: services haute-performance (notifications, workers, streaming).

Technologies: Go

Architecture: petits microservices, communication via Redis / broker.

Démarrage (placeholder):
- `go run ./services/notifications`
- `go run ./services/workers`
- `go run ./services/streaming`

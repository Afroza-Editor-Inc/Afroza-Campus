# NestJS — Core API

Rôle: API GraphQL principale et REST admin, logique métier, auth, profiles, feed.

Technologies: Node.js, TypeScript, NestJS, GraphQL (Apollo)

Démarrage (placeholder):
1. `npm install`
2. `npm run start:dev`

Communication:
- Expose GraphQL pour clients mobile/web
- Reçoit et publie events vers Phoenix / Go services

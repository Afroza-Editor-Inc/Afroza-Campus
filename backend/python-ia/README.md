# Python — IA & Data (FastAPI)

Rôle: endpoints ML & IA: recommandations, modération, chatbot minimal.

Technologies: Python, FastAPI

Démarrage (placeholder):
1. Créer un virtualenv
2. `pip install -r requirements.txt`
3. `uvicorn app.main:app --reload --port 5000`

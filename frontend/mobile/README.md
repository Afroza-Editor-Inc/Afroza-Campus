# Frontend Mobile — Afroza Campus

Rôle: Client mobile principal, experience mobile-first pour Android et iOS.
Technologies: React Native, TypeScript

Comment lancer (placeholder):
- Installer dépendances: `npm install`
- Démarrer: `npm run start`

Communication:
- Appelle API via `nestjs-core` (REST/GraphQL)
- Reçoit événements temps réel via Phoenix channels (WebSocket)

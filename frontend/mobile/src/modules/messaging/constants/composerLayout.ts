/** Hauteurs estimées du dock de composition (px) — utilisées pour le padding liste + positionnement. */
// Rangée composer = paddingVertical (5*2) + composerShell (38) ≈ 48px.
export const COMPOSER_ROW_HEIGHT = 48;
export const EMOJI_PANEL_HEIGHT = 288;
export const REPLY_BANNER_HEIGHT = 36;
export const ATTACHMENT_ROW_HEIGHT = 30;
export const PANEL_TRANSITION_MS = 200;

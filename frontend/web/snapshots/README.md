Snapshots for web UI. Place screenshots here (PNG) to track visual progress.
- snapshot-home.png
- snapshot-profile.png

To capture: run `yarn dev` and open `http://localhost:3000` then take screenshots.